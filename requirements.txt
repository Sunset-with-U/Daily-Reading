# 运行时依赖（管线）
# 注：不用 feedparser —— 其依赖 sgmllib3k 在 setuptools>=78 下无法构建，
#     RSS/Atom 解析由 pipeline/fetch/feedxml.py（基于 lxml）承担
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
PyYAML>=6.0
python-dateutil>=2.9
anthropic>=0.116
pandas>=2.2
openpyxl>=3.1
yfinance>=0.2.50
